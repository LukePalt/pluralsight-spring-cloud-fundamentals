spring:
  application:
    name: config-client-application
  cloud:
    config:
      discovery:
        enabled: true

eureka:
  client:
    server-url:
      default-zone: http://localhost:8761/eureka

server:
  port: 8082